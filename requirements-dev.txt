# Use this to install a development environment for yawt. Note that we
# depend on the testing envionment, with some added goodies.

-r requirements-test.txt

nosy==1.1.2
pinocchio==0.4.2
rope==0.10.2
ropemacs==0.7
ropemode==0.2
pylint==1.3.1
pep8==1.5.7
